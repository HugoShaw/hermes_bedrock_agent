"""
Hermes Bedrock Agent V2 — Business Semantic Graph + Implementation Graph + Vector Evidence Store.

This package implements the V2 refactored architecture:
- Evidence-centric Vector Store (not chunk-driven)
- Business Semantic Graph (domain, process, step, rule, term, function, screen, role, org)
- Implementation Graph (system, module, API, service, class, method, table, column, SQL, job, file)
- Evidence Linker (graph nodes/edges → evidence chunks)

Run ID: murata_semantic_v2
Dataset: murata
"""

__version__ = "2.0.0"
